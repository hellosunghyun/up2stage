# References

이 폴더는 개발 근거를 보관하는 읽기 전용 영역이다.

- 제품 코드에서 import하지 않는다.
- Extension 번들에 포함하지 않는다.
- 원본 파일을 수정·정리·포맷팅하지 않는다.
- legacy `Unfold` 명칭이 포함되어도 원본을 보존한다.
- 새 구현에는 현재 docs의 `UP²STAGE` 결정을 적용한다.
- 외부 문서와 발표 자료의 재배포 범위는 별도 확인한다.
